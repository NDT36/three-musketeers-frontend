import React, { FC } from 'react';

interface IProps {}

const LendPage: FC<IProps> = (props) => {
  return (
    <div className="px-2">
      <div>Lend page</div>
    </div>
  );
};

export default LendPage;
