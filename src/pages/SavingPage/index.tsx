import React, { FC } from 'react';

interface IProps {}

const SavingPage: FC<IProps> = (props) => {
  return (
    <div className="px-2">
      <div>Saving page</div>
    </div>
  );
};

export default SavingPage;
